
import { TaskList } from '@/components/TaskList';
import { Layout } from '@/components/Layout';

const Tasks = () => {
  return (
    <Layout>
      <TaskList />
    </Layout>
  );
};

export default Tasks;
